{
  "name": "pm-context",
  "version": "2026.8.24",
  "description": "Generate deterministic pm context packs for agent handoffs, reviews, and status briefs",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "manifest.json",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build && npm run build:test && node --test test/*.test.ts",
    "prepack": "npm run build",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck",
    "audit:prod": "node --input-type=module -e \"import { spawnSync } from 'node:child_process'; import { devNull } from 'node:os'; const env = { ...process.env, npm_config_userconfig: devNull, NPM_CONFIG_USERCONFIG: devNull }; for (const key of Object.keys(env)) if (key.toLowerCase() === 'npm_config_allow_scripts') delete env[key]; const win = process.platform === 'win32'; const r = spawnSync(win ? 'npm.cmd' : 'npm', ['audit', '--omit=dev', '--ignore-scripts'], { stdio: 'inherit', env, shell: win }); process.exit(r.status ?? 1);\"",
    "pack:dry-run": "npm pack --dry-run",
    "changelog:full": "pm-changelog --pm-root .agents/pm --mode replace --output CHANGELOG.md --all-release-tags --release-version-from-package --item-url-base https://github.com/unbraind/pm-context/blob/main/.agents/pm --respect-item-release --pm-bin ./node_modules/.bin/pm --pm-arg=--output-budget --pm-arg=unbounded --pm-arg=--output-limit --pm-arg=unbounded",
    "changelog:check": "npm run changelog:full -- --check",
    "release:check": "npm run typecheck && npm run docstring && npm run build && npm run coverage && npm run audit:prod && npm run pack:dry-run",
    "prepublishOnly": "npm run release:check",
    "prepare": "node scripts/prepare-merge-driver.mjs",
    "merge:install": "pm merge install",
    "coverage": "npm run build && npm run build:test && node scripts/coverage-gate.ts",
    "docstring": "node scripts/docstring-gate.ts"
  },
  "keywords": [
    "pm-cli",
    "pm-package",
    "context",
    "handoff",
    "project-management"
  ],
  "author": "@unbraind",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unbraind/pm-context.git"
  },
  "homepage": "https://github.com/unbraind/pm-context#readme",
  "bugs": {
    "url": "https://github.com/unbraind/pm-context/issues"
  },
  "peerDependencies": {
    "@unbrained/pm-cli": ">=2026.8.15"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@unbrained/pm-cli": "2026.8.19",
    "c8": "^12.0.0",
    "pm-changelog": "^2026.8.7",
    "pm-ops": "2026.8.17",
    "typescript": "^7.0.2"
  },
  "pmPackage": {
    "name": "pm-context",
    "displayName": "pm-context",
    "description": "Generate deterministic pm context packs for agent handoffs, reviews, and status briefs",
    "repository": "github.com/unbraind/pm-context",
    "install": "pm install github.com/unbraind/pm-context --project",
    "capabilities": [
      "commands",
      "renderers",
      "schema"
    ]
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "coverageGate": {
    "sources": [
      ".",
      "scripts/coverage-gate.ts",
      "scripts/docstring-gate.ts"
    ],
    "tests": [
      "test/*.test.ts"
    ],
    "thresholds": {
      "lines": 100,
      "branches": 100,
      "functions": 100
    },
    "ignore": []
  }
}
